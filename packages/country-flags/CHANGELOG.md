# @radianui/flags

## 0.0.3

### Patch Changes

- Include the canonical flag manifest and SVG source files in the published package.

## 0.0.2

### Patch Changes

- a2e8a67: Inline runtime fallback in `Flag` component and remove `placeholder` entry from `manifest.json`
- Export the complete typed flag catalog from `@radianui/flags/metadata`.
- cb726c4: Expose `./source/*` in package exports for raw SVG asset access
