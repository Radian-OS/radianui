---
title: Color Picker
description: For color selection
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/ui/color-picker.tsx
  externalReference:
    - label: Culori
      href: https://culorijs.org/
      icon: culori.webp
---

<ColorPickerPreview />

## Installation

<Tabs defaultValue="cli">
	<TabsList>
		<TabsTrigger value="cli">CLI</TabsTrigger>
		<TabsTrigger value="manual">Manual</TabsTrigger>
	</TabsList>
	<TabsContent value="cli" className="p-0">
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm dlx @radianos/radianbeta add color-picker",
				npm: "npx @radianos/radianbeta add color-picker",
				yarn: "yarn @radianos/radianbeta add color-picker",
				bun: "bunx @radianos/radianbeta add color-picker",
			}}
		/>
	</TabsContent>
	<TabsContent value="manual" className="p-0">
	<Steps>
		<Step>Install the following dependencies:</Step>
    	<PackageManagerTabs
    		commands={{
    			pnpm: "pnpm add culori",
    			npm: "npm install culori",
    			yarn: "yarn add culori",
    			bun: "bun add culori",
    		}}
    	/>

    	<Step>Copy and paste the following code into your project.</Step>
    	<ComponentSource name="color-picker" title="components/ui/color-picker.tsx" collapsible />

    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </TabsContent>

</Tabs>

## Usage

### Imports

<CodeSnippet title="/components/ui/color-picker" code={`import { ColorPicker } from "@/components/ui/color-picker"`} />

### Code

<CodeSnippet title="/components/ui/color-picker" code={`<ColorPicker label="Select Color" defaultColor="#461EFA" />`} />

---

## Example

### Color Picker Example

<ColorPickerExample />

---

## Props

<PropsTableWrapper>
	<PropsTable
		title="ColorPicker"
		data={[
			{
				name: "label",
				values: ["string"],
				required: false,
				description: "The label for the color picker",
			},
			{
				name: "size",
				values: ["28", "32", "36", "40", "44", "48"],
				defaultValue: "32",
				required: false,
				description: "The size of the color picker",
			},
			{
				name: "disabled",
				values: ["boolean"],
				defaultValue: "false",
				required: false,
				description: "Whether the color picker is disabled",
			},
			{
				name: "hint",
				values: ["string"],
				required: false,
				description: "A hint or additional information for the color picker",
			},
            {
                name:"hasError",
                values: ["boolean"],
                defaultValue: "false",
                required: false,
                description: "Indicates if the color picker has an error state",
            },
            {
                name:"defaultColor",
                values: ["string"],
                required: false,
                description: "The default color value for the color picker",
            }

    	]}
    />

</PropsTableWrapper>
