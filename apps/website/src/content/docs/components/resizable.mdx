---
title: Resizable
description: Resizable panel groups and layouts with keyboard support
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/ui/resizable.tsx
  externalReference:
    - label: react-resizable-panels
      href: https://www.npmjs.com/package/react-resizable-panels
      icon: npm.svg
---

<ResizablePreview />

## Installation

<Tabs defaultValue="cli">
	<TabsList>
		<TabsTrigger value="cli">CLI</TabsTrigger>
		<TabsTrigger value="manual">Manual</TabsTrigger>
	</TabsList>
	<TabsContent value="cli" className="p-0">
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm dlx @radianos/radianbeta add resizable",
				npm: "npx @radianos/radianbeta add resizable",
				yarn: "yarn @radianos/radianbeta add resizable",
				bun: "bunx @radianos/radianbeta add resizable",
			}}
		/>
	</TabsContent>
	<TabsContent value="manual" className="p-0">
	<Steps>
	    <Step>Install the following dependencies:</Step>
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm add @radix-ui/react-resizable",
				npm: "npm install @radix-ui/react-resizable",
				yarn: "yarn add @radix-ui/react-resizable",
				bun: "bun add @radix-ui/react-resizable",
			}}
		/>

    	<Step>Copy and paste the following code into your project.</Step>
    	<ComponentSource name="resizable" title="components/ui/resizable.tsx" collapsible />

    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </TabsContent>

</Tabs>

## Usage

### Imports

<CodeSnippet title="/components/ui/resizable" code={`import {ResizableHandle, ResizablePanel, ResizablePanelGroup } from "@/components/ui/resizable"`} />

### Code

<CodeSnippet title="/components/ui/resizable" code={`import {ResizableHandle, ResizablePanel, ResizablePanelGroup } from "@/components/ui/resizable"`} />

---

## Props

<PropsTableWrapper>
	<PropsTable
		title="ResizablePanelGroup"
		data={[
			{
				name: "direction",
				values: ["top", "bottom", "left", "right"],
				defaultValue: "right",
				description: "Adds direction where content is dragged",
			},
		]}
	/>
	<PropsTable
		title="ResizablePanel"
		data={[
			{
				name: "defaultSize",
				values: ["number"],
				description: "Controls size of containers",
			},
		]}
	/>
	<PropsTable
		title="ResizableHandle"
		data={[
			{
				name: "withHandle",
				values: ["boolean"],
				defaultValue: "false",
				description: "Toggles Handle",
			},
		]}
	/>
</PropsTableWrapper>
