---
title: File Upload
description: Allow users to upload files
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/ui/file-upload.tsx
---

<FileUploadPreview />

## Installation

<Tabs defaultValue="cli">
	<TabsList>
		<TabsTrigger value="cli">CLI</TabsTrigger>
		<TabsTrigger value="manual">Manual</TabsTrigger>
	</TabsList>
	<TabsContent value="cli" className="p-0">
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm dlx @radianos/radianbeta add file-upload",
				npm: "npx @radianos/radianbeta add file-upload",
				yarn: "yarn @radianos/radianbeta add file-upload",
				bun: "bunx @radianos/radianbeta add file-upload",
			}}
		/>
	</TabsContent>
	<TabsContent value="manual" className="p-0">
	<Steps>
	    <Step>Install the following dependencies:</Step>
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm add @radix-ui/react-file-upload",
				npm: "npm install @radix-ui/react-file-upload",
				yarn: "yarn add @radix-ui/react-file-upload",
				bun: "bun add @radix-ui/react-file-upload",
			}}
		/>

    	<Step>Copy and paste the following code into your project.</Step>
    	<ComponentSource name="file-upload" title="components/ui/file-upload.tsx" collapsible />

    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </TabsContent>

</Tabs>

## Usage

### Imports

<CodeSnippet title="/components/ui/file-upload.tsx" code={`import {FileUpload} from "@/components/ui/file-upload"`} />

### Code

<CodeSnippet
	title="/components/ui/file-upload.tsx"
	code={`<FileUpload
		title="Drag and drop files to upload"
		description="JPG, PNG, GIF or other image files"
		className="w-80"
		variant="input"
		accept="image/*"
/>`}
/>

---

## Props

<PropsTableWrapper>
	<PropsTable
		title="FileUpload"
		data={[
			{
				name: "label",
				description: "The label for the file upload.",
				type: "string",
				values: ["string"],
			},
			{
				name: "placeholder",
				description: "The placeholder text for the file upload.",
				values: ["string"],
			},
			{
				name: "size",
				defaultValue: "36",
				description: "The size of the file upload.",
				values: ["28", "32", "36", "40", "44", "48"],
			},
			{
				name: "rounded",
				defaultValue: "lg",
				description: "The border radius of the file upload.",
				values: ["xs", "sm", "md", "lg", "xl", "2xl"],
			},
			{
				name: "disabled",
				defaultValue: "false",
				description: "Whether the file upload is disabled.",
				values: ["boolean"],
			},
			{
				name: "hasError",
				defaultValue: "false",
				description: "Whether the file upload has an error.",
				values: ["boolean"],
			},
			{
				name: "hint",
				description: "The hint text for the file upload.",
				values: ["string"],
			},
			{
				name: "variant",
				description: "The variant of the file upload.",
				defaultValue: "input",
				values: ["input", "container"],
				required: true,
			},
			{
				name: "accept",
				description: "The accepted file types for the file upload.",
				values: ["string"],
			},
			{
				name: "maxSize",
				description: "The maximum file size in bytes.",
				values: ["number"],
			},
			{
				name: "maxFiles",
				description: "The maximum number of files that can be uploaded.",
				values: ["number"],
				defaultValue: "4",
			},
		]}
	/>
</PropsTableWrapper>
