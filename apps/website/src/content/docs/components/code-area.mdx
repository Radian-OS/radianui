---
title: Code Area
description: Display code snippets with syntax highlighting and formatting
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/ui/code-area.tsx
  externalReference:
    - label: Shiki
      href: https://shiki.style/
      icon: shiki.svg
---

<ComponentPreview path="code-area/code-area-preview" />

## Installation

<Tabs defaultValue="cli">
	<TabsList>
		<TabsTrigger value="cli">CLI</TabsTrigger>
		<TabsTrigger value="manual">Manual</TabsTrigger>
	</TabsList>
	<TabsContent value="cli" className="p-0">
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm dlx @radianos/radianbeta add code-area",
				npm: "npx @radianos/radianbeta add code-area",
				yarn: "yarn @radianos/radianbeta add code-area",
				bun: "bunx @radianos/radianbeta add code-area",
			}}
		/>
	</TabsContent>
	<TabsContent value="manual" className="p-0">
	<Steps>
		<Step>Install the following dependencies:</Step>
    	<PackageManagerTabs
    		commands={{
    			pnpm: "pnpm add react-shiki @radix-ui/react-slot",
    			npm: "npm install react-shiki @radix-ui/react-slot",
    			yarn: "yarn add react-shiki @radix-ui/react-slot",
    			bun: "bun add react-shiki @radix-ui/react-slot",
    		}}
    	/>
		
    	<Step>Copy and paste the following code into your project.</Step>
		<Tabs defaultValue="code-area">
    		<TabsList>
    			<TabsTrigger value="code-area">Code Area</TabsTrigger>
    			<TabsTrigger value="button">Button</TabsTrigger>
    		</TabsList>
    		<TabsContent value="code-area">
    			<ComponentSource name="code-area" title="components/ui/code-area.tsx" collapsible />
    		</TabsContent>
    		<TabsContent value="button">
    			<ComponentSource name="button" title="components/ui/button.tsx" collapsible />
    		</TabsContent>
    	</Tabs>

    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </TabsContent>

</Tabs>

## Usage

### Imports

<CodeSnippet title="/components/ui/code-area" code={`import { CodeArea } from "@/components/ui/code-area"`} />

### Code

<CodeSnippet
	title="/components/ui/code-area"
	code={`
<CodeArea 
  code={'const Greeting: React.FC<{ name: string }> = ({ name }) => {\n  return <h1>Hello, {name}!</h1>;\n};'}
  language="tsx"
  theme="github-dark-default"
  lineNumbers={false}
  className="h-full w-full"
/>
`}
/>

---

## Example

### Code Area with Tabs

<ComponentPreview path="code-area/code-with-tabs-preview" />

### Code Area with Copy Button

<ComponentPreview path="code-area/code-with-copy-example" />

---

## Props

<PropsTableWrapper>
	<PropsTable
		title="CodeArea"
		data={[
			{
				name: "code",
				values: ["string"],
				required: true,
				description: "The code snippet to display",
			},
			{
				name: "language",
				values: ["typescript", "javascript", "python", "java", "csharp", "html", "css", "json", "markdown", "bash", "..."],
				required: false,
				description: "The programming language for syntax highlighting",
			},
			{
				name: "theme",
				defaultValue: "github-dark-high-contrast",
				values: ["github-dark-high-contrast", "nord", "dracula", "solarized-light", "solarized-dark", "..."],
				required: false,
				description: "The theme for syntax highlighting",
			},
			{
				name: "showLineNumbers",
				defaultValue: "false",
				values: ["boolean"],
				required: false,
				description: "Whether to show line numbers in the code area",
			},
			{
				name: "copiable",
				defaultValue: "false",
				values: ["boolean"],
				required: false,
				description: "If true, the code can be copied to clipboard",
			},
		]}
		externalReference="https://shiki.style/"
	/>
</PropsTableWrapper>
