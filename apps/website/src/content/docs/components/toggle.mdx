---
title: Toggle
description: A simple button that stays highlighted when clicked, perfect for turning formatting styles on and off.
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/ui/toggle.tsx
  figma:
    href: https://www.figma.com/community/file/1601125934366184350/radian-design-system-version-0-1-2
  externalReference:
    - label: Radix  UI
      href: https://www.radix-ui.com/primitives/docs/components/toggle
---

<ComponentPreview path="toggle/toggle-preview" />

---

## Installation

<InstallationTabs>
	<InstallTabsList>
		<InstallTabsTrigger value="cli">CLI</InstallTabsTrigger>
		<InstallTabsTrigger value="manual">Manual</InstallTabsTrigger>
	</InstallTabsList>
	<InstallTabsContent value="cli">
		```bash
		npx radianui@latest add toggle
		```
	</InstallTabsContent>
	<InstallTabsContent value="manual" className="p-0">
	<Steps>
	    <Step>Install the following dependencies:</Step>
		```bash
		npm install @radix-ui/react-toggle
		```
    	<Step>Copy and paste the following code into your project.</Step>
    	<ComponentSource name="toggle" title="components/ui/toggle.tsx" collapsible />

    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </InstallTabsContent>

</InstallationTabs>

---

## Usage

### Imports

```tsx showLineNumbers title="/components/ui/toggle.tsx"
import { Toggle } from "@/components/ui/toggle"
```

### Code

```tsx showLineNumbers title="/components/ui/toggle.tsx"
<Toggle>Toggle</Toggle>
```

---

## Examples

<ComponentPreview title="Variants" path="toggle/toggle-variants" />

<ComponentPreview title="Sizes" path="toggle/toggle-sizes" />

<ComponentPreview title="Favorite" path="toggle/toggle-favorite" />

<ComponentPreview
	title="Formatting Toolbar"
	path="toggle/toggle-formatting-toolbar"
/>

<ComponentPreview title="Notifications" path="toggle/toggle-notification" />

<ComponentPreview title="With Heart" path="toggle/toggle-heart" />

<ComponentPreview title="Disabled" path="toggle/toggle-disabled" />

---

<ComponentDocContent
	name={"Toggle"}
	description={
		"Toggle is a button that lets users switch between two states, such as on/off or active/inactive. It provides accessible interactions, keyboard support, and customizable styles for actions like bold, italic, or favorite."
	}
	features={[
		"@radix-ui/react-toggle, ensures strict ARIA compliance with correct aria-pressed attribute management.",
		"Natively supports keyboard interaction; users can toggle the state by pressing Space or Enter when the button is focused.",
		"Exposes a highly composable API: <Toggle> and various variants for different visual styles (like outline, ghost, or solid).",
		"Effortlessly styled with Tailwind CSS, utilizing data-[state=on] modifiers to smoothly transition background, text, and border colors.",
		"Unopinionated structural foundation, making it trivial to nest icons (from Lucide or other sets) directly inside the component to create rich toolbars.",
	]}
	related={[
		{
			title: "Switch",
			description:
				'Use a Switch for binary preference settings (like "Enable Notifications"). Use a Toggle for toolbar-style actions (like "Bold" text or "Filter Results").',
			href: "/docs/components/switch",
		},
		{
			title: "Button",
			description:
				"The core aesthetic foundation. The Toggle component effectively behaves like a stateful, persistent-pressed Button.",
			href: "/docs/components/button",
		},
		{
			title: "Toggle Group",
			description:
				"A collection of Toggles that manage a set of options (e.g., text alignment: Left, Center, Right), often used in text editors.",
			href: "/docs/components/toggle-group",
		},
	]}
	faqs={[
		{
			question: "How do I manage the Toggle state?",
			answer:
				"You manage the boolean state in your parent component and pass it to the pressed prop. You should then listen to the onPressedChange callback to update your state whenever the user clicks or toggles the button.",
		},
		{
			question: "How do I change the visual appearance when it is toggled?",
			answer:
				"The Radix primitive adds a data-[state=on] attribute to the component. You can style this using Tailwind's data-state modifiers. For example, data-[state=on]:bg-primary data-[state=on]:text-primary-foreground will automatically trigger these styles whenever the button is pressed.",
		},
		{
			question: "How do I integrate this with React Hook Form?",
			answer:
				'Like other custom primitives, wrap the <Toggle> inside an RHF <Controller>. Map the onPressedChange callback directly to the field.onChange handler to ensure your form state accurately reflects whether the toggle is currently "on" or "off."',
		},
		{
			question: "Can I group multiple Toggles together?",
			answer:
				'Yes. You can use the Radian ToggleGroup component, which manages a set of Toggles and allows you to enforce "single-select" or "multi-select" behavior within that group context.',
		},
		{
			question: "Why isn't the toggle responding to clicks?",
			answer:
				"Ensure that the component is not being blocked by a parent pointer-events-none class. Also, check if you have disabled the button in your props (e.g., disabled={true}), which prevents the Radix primitive from registering any interactive events.",
		},
	]}
/>

## Props

<PropsTableWrapper>
	<PropsTable
		title="Toggle"
		data={[
			{
				name: "variant",
				defaultValue: "outline",
				values: ["ghost", "outline"],
				required: false,
				description: "Defines the visual style of the toggle",
			},
			{
				name: "size",
				defaultValue: "32",
				values: ["28", "32", "36", "40", "44", "48"],
				required: false,
				description: "Sets the size of the toggle",
			},
			{
				name: "pressed",
				defaultValue: "undefined",
				values: ["boolean"],
				required: false,
				description: "Controls the pressed state of the toggle",
			},
			{
				name: "defaultPressed",
				defaultValue: "false",
				values: ["boolean"],
				required: false,
				description:
					"Sets the initial pressed state for an uncontrolled toggle",
			},
			{
				name: "onPressedChange",
				defaultValue: "-",
				values: ["(pressed: boolean) => void"],
				required: false,
				description: "Callback fired when the pressed state changes",
			},
			{
				name: "disabled",
				defaultValue: "false",
				values: ["boolean"],
				required: false,
				description: "Disables interaction with the toggle when set to true",
			},
			{
				name: "asChild",
				defaultValue: "false",
				values: ["boolean"],
				required: false,
				description: "Use this prop to render the toggle as a child component",
			},
		]}
		externalReference="https://www.radix-ui.com/primitives/docs/components/toggle#root"
	/>
</PropsTableWrapper>
