---
title: Input OTP
description: A group of single-character text inputs to handle one-time password verification
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/ui/input-otp.tsx
  externalReference:
    - label: input-otp
      href: https://www.npmjs.com/package/input-otp
      icon: npm.svg
---

<InputOtpPreview />

## Installation

<Tabs defaultValue="cli">
	<TabsList>
		<TabsTrigger value="cli">CLI</TabsTrigger>
		<TabsTrigger value="manual">Manual</TabsTrigger>
	</TabsList>
	<TabsContent value="cli" className="p-0">
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm dlx @radianos/radianbeta add input-otp",
				npm: "npx @radianos/radianbeta add input-otp",
				yarn: "yarn @radianos/radianbeta add input-otp",
				bun: "bunx @radianos/radianbeta add input-otp",
			}}
		/>
	</TabsContent>
	<TabsContent value="manual" className="p-0">
	<Steps>
	    <Step>Install the following dependencies:</Step>
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm add input-otp @radix-ui/react-label",
				npm: "npm install input-otp @radix-ui/react-label",
				yarn: "yarn add input-otp @radix-ui/react-label",
				bun: "bun add input-otp @radix-ui/react-label",
			}}
		/>

    	<Step>Copy and paste the following code into your project.</Step>
    	<Tabs defaultValue="input-otp">
    		<TabsList>
    			<TabsTrigger value="input-otp">Input OTP</TabsTrigger>
    			<TabsTrigger value="label">Label</TabsTrigger>
    		</TabsList>
    		<TabsContent value="input-otp">
    			<ComponentSource name="input-otp" title="components/ui/input-otp.tsx" collapsible />
    		</TabsContent>
    		<TabsContent value="label">
    			<ComponentSource name="label" title="components/ui/label.tsx" collapsible />
    		</TabsContent>
    	</Tabs>

    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </TabsContent>

</Tabs>

## Usage

### Imports

<CodeSnippet title="/components/ui/input-otp.tsx" code={`import { InputOtp } from "@/components/ui/input-otp"`} showLineNumbers={false} />

### Code

<CodeSnippet title="/components/ui/input-otp.tsx" code={`<InputOtp />`} showLineNumbers={false} />

---

## Props

<PropsTableWrapper>
	<PropsTable
		title="InputOtp"
		data={[
			{
				name: "size",
				description: "Sets the size of OTP Input",
				reauired: false,
				defaultValue: "40",
				values: ["28", "32", "36", "40", "44", "56"],
			},
			{
				name: "disabled",
				description: "Sets the disabled state",
				required: false,
				defaultValue: "false",
				values: ["boolean"],
			},
			{
				name: "length",
				description: "Sets the number of boxes",
				required: false,
				defaultValue: "6",
				values: ["number"],
			},
			{
				name: "label",
				description: "Sets the label for Input OTP",
				required: false,
				values: ["string"],
			},
			{
				name: "hasError",
				description: "Sets the error state for Input OTP",
				required: false,
				defaultValue: "false",
				values: ["boolean"],
			},
            {
                name:"errorMsg",
                description:"Sets the errorMsg for the Input OTP",
                required:false,
                values:["string"]

            }
    	]}
    />

</PropsTableWrapper>
