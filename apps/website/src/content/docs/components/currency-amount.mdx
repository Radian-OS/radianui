---
title: Currency Amount
description: Display currency amount
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/ui/currency.tsx
---

<CurrencyAmountPreview />

## Installation

<Tabs defaultValue="cli">
	<TabsList>
		<TabsTrigger value="cli">CLI</TabsTrigger>
		<TabsTrigger value="manual">Manual</TabsTrigger>
	</TabsList>
	<TabsContent value="cli" className="p-0">
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm dlx @radianos/radianbeta add currency-amount",
				npm: "npx @radianos/radianbeta add currency-amount",
				yarn: "yarn @radianos/radianbeta add currency-amount",
				bun: "bunx @radianos/radianbeta add currency-amount",
			}}
		/>
	</TabsContent>
	<TabsContent value="manual" className="p-0">
	<Steps>
    	<Step>Copy and paste the following code into your project.</Step>
    	<ComponentSource name="currency-amount" title="components/ui/currency-amount.tsx" collapsible />

    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </TabsContent>

</Tabs>

## Usage

### Imports

<CodeSnippet title="/registry/ui/currency" code={`import { CurrencyInput } from "@/registry/ui/currency"`} />

### Code

<CodeSnippet title="/registry/ui/currency" code={`<CurrencyInput currency="usd" />`} />

---

## Example

### Currency With Select Example

<CurrencyExamplePreview />

---

## Props

<PropsTableWrapper>
	<PropsTable
		title="CurrencyInput"
		data={[
			{
				name: "label",
				values: ["string"],
				required: false,
				description: "The label for the currency input",
			},
			{
				name: "size",
				values: ["28", "32", "36", "40", "44", "48"],
				defaultValue: "36",
				required: false,
				description: "The size of the currency input",
			},
			{
				name: "rounded",
				values: ["xs", "sm", "md", "lg", "xl", "2xl"],
				defaultValue: "lg",
				required: false,
				description: "The border radius of the currency input",
			},
			{
				name: "disabled",
				values: ["boolean"],
				defaultValue: "false",
				required: false,
				description: "Whether the currency input is disabled",
			},
			{
				name: "hint",
				values: ["string"],
				required: false,
				description: "A hint or additional information for the currency input",
			},
			{
				name: "hasError",
				values: ["boolean"],
				defaultValue: "false",
				required: false,
				description: "Indicates if the currency input has an error state",
			},
			{
				name: "currency",
				values: ["string"],
				defaultValue: "USD",
				required: false,
				description: "The currency code for the input, e.g., 'USD', 'EUR', 'JPY'",
			},
			{
				name: "locale",
				defaultValue: "en-US",
				values: ["string"],
				required: false,
				description: "The locale for the currency input",
			},
			{
				name: "separator",
				values: ["boolean"],
				defaultValue: "true",
				required: false,
				description: "The separator for the currency input, e.g., ',', '.'",
			},
			{
				name: "decimalPlaces",
				values: ["number"],
				defaultValue: "2",
				required: false,
				description: "The number of decimal places to display in the currency input",
			},
			{
				name: "maxValue",
				values: ["number"],
				required: false,
				description: "The maximum value for the currency input",
			},
		]}
	/>
</PropsTableWrapper>
