---
title: Table
description: Display tabular data with sorting and pagination
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/ui/table.tsx
---

<ComponentPreview path="table/table-preview" />

## Installation

<Tabs defaultValue="cli">
	<TabsList>
		<TabsTrigger value="cli">CLI</TabsTrigger>
		<TabsTrigger value="manual">Manual</TabsTrigger>
	</TabsList>
	<TabsContent value="cli" className="p-0">
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm dlx @radianos/radianbeta add table",
				npm: "npx @radianos/radianbeta add table",
				yarn: "yarn @radianos/radianbeta add table",
				bun: "bunx @radianos/radianbeta add table",
			}}
		/>
	</TabsContent>
	<TabsContent value="manual" className="p-0">
	<Steps>

    	<Step>Copy and paste the following code into your project.</Step>
    	<Tabs defaultValue="table">
    		<TabsList>
    			<TabsTrigger value="table">Table</TabsTrigger>
    		</TabsList>
    		<TabsContent value="table">
    			<ComponentSource name="table" title="components/ui/table.tsx" collapsible />
    		</TabsContent>
    	</Tabs>

    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </TabsContent>

</Tabs>

## Usage

### Imports

<CodeSnippet
	title="/components/ui/table"
	code={`import {
  Table,
  TableBody,
  TableCaption,
  TableCell,
  TableFooter,
  TableHead,
  TableHeader,
  TableRow,
} from '@/components/ui/table'`}
/>

---

### Code

<CodeSnippet
	title="Usage"
	code={`<Table>
	<TableCaption>User Table</TableCaption>
	<TableHeader>
		<TableRow>
			<TableHead>Name</TableHead>
		</TableRow>
	</TableHeader>
	<TableBody>
		<TableRow>
			<TableCell>Radian</TableCell>
		</TableRow>
	</TableBody>
</Table>`}
/>

---

## Data Table

Every data table or data grid is unique. Each one has different behaviors, sorting and filtering logic, and data sources.

<br />
Because of that, it doesn’t make sense to wrap all of these variations into a single, reusable component — doing so would sacrifice the flexibility that headless UI libraries
provide.
<br />
Instead, we use [TanStack Table](https://tanstack.com/table/latest) as the core logic layer and pair it with our [Table](/docs/components/table) component for the UI. This approach
preserves flexibility while maintaining a consistent look and feel.
<br />
The examples below showcase different ways to build data tables using [TanStack Table](/docs/components/table). Some examples also integrate [dnd-kit](/docs/components/draggable)
to enable draggable and re-orderable rows or columns.
<br />
**Note:** [TanStack Table](https://tanstack.com/table/latest) is a required dependency for all implementations. [dnd-kit](https://dndkit.com/) is only needed for examples that
include drag-and-drop functionality.

### Basic Data Table

<ComponentPreview path="table/basic-data-table" />

---

## Examples

### Expand Table

<ComponentPreview path="table/expand-table" />

### Resizable Table

<ComponentPreview path="table/resize-table" />

### Draggable Table

<ComponentPreview path="table/dragable-table" />

### Employee Dashboard Table

<ComponentPreview path="table/table-example" />

---

## Props

<PropsTableWrapper>
	<PropsTable
		title="Table"
		data={[
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Styling for the table container.",
			},
		]}
	/>
	<PropsTable
		title="TableHeader"
		data={[
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Styling for the table header.",
			},
		]}
	/>
	<PropsTable
		title="TableRow"
		data={[
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Styling for the table row.",
			},
		]}
	/>
	<PropsTable
		title="TableHead"
		data={[
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Styling for the table head.",
			},
		]}
	/>
	<PropsTable
		title="TableBody"
		data={[
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Styling for the table body.",
			},
		]}
	/>
	<PropsTable
		title="TableCell"
		data={[
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Styling for the table cell.",
			},
		]}
	/>
	<PropsTable
		title="TableFooter"
		data={[
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Styling for the table footer.",
			},
		]}
	/>
	<PropsTable
		title="TableCaption"
		data={[
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Styling for the table caption.",
			},
		]}
	/>
</PropsTableWrapper>
