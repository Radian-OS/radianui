---
title: Select
description: A clean and stylized dropdown selection menu to replace ugly, default browser input lists.
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/ui/select.tsx
  figma:
    href: https://www.figma.com/community/file/1601125934366184350/radian-design-system-version-0-1-2
  externalReference:
    - label: Radix UI
      href: https://www.radix-ui.com/primitives/docs/components/select
---

<ComponentPreview path="select/select-preview" />

---

## Installation

<InstallationTabs defaultValue="cli">
	<InstallTabsList>
		<InstallTabsTrigger value="cli">CLI</InstallTabsTrigger>
		<InstallTabsTrigger value="manual">Manual</InstallTabsTrigger>
	</InstallTabsList>
	<InstallTabsContent value="cli" className="p-0">
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm dlx radianui@latest add select",
				npm: "npx radianui@latest add select",
				yarn: "yarn dlx radianui@latest add select",
				bun: "bunx radianui@latest add select",
			}}
		/>
	</InstallTabsContent>
	<InstallTabsContent value="manual" className="p-0">
	<Steps>
	    <Step>Install the following dependencies:</Step>
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm add @radix-ui/react-select",
				npm: "npm install @radix-ui/react-select",
				yarn: "yarn add @radix-ui/react-select",
				bun: "bun add @radix-ui/react-select",
			}}
		/>

    	<Step>Copy and paste the following code into your project.</Step>
    	<ComponentSource name="select" title="components/ui/select.tsx" collapsible />

    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </InstallTabsContent>

</InstallationTabs>

---

## Usage

### Imports

```tsx showLineNumbers title="/components/ui/select.tsx"
import {
	Select,
	SelectContent,
	SelectItem,
	SelectTrigger,
	SelectValue,
} from "@/components/ui/select"
```

### Code

```tsx
<Select>
	<SelectTrigger>
		<SelectValue />
	</SelectTrigger>
	<SelectContent>
		<SelectItem value="male">Male</SelectItem>
		<SelectItem value="female">Female</SelectItem>
	</SelectContent>
</Select>
```

---

## Examples

<ComponentPreview title="Size" path="select/select-sizes" />

<ComponentPreview title="With Badge" path="select/select-with-badge" />

<ComponentPreview title="With Avatar" path="select/select-with-avatar" />

<ComponentPreview title="Custom indicator" path="select/custom-indicator" />

<ComponentPreview title="Indicator Position" path="select/indicator-position" />

<ComponentPreview title="Time Picker" path="select/time-picker" />

<ComponentPreview title="Form" path="select/select-form" />

---

<ComponentDocContent
	name={"Select"}
	description={
		"Select is a form component that lets users choose a single option from a dropdown list. It provides smooth keyboard navigation, easily customizable dropdowns, and a consistent experience across browsers and devices."
	}
	features={[
		'@radix-ui/react-select, ensures native typeahead support (e.g., typing "C" jumps to "Canada") and seamless keyboard navigation using arrow keys.',
		"Renders the option list inside a React Portal, guaranteeing it escapes any parent container overflow-hidden constraints.",
		"Modular layout pieces: <Select>, <SelectTrigger>, <SelectValue>, <SelectContent>, <SelectGroup>, <SelectItem> and so on.",
		'Natively supports the position="popper" configuration, dynamically flipping the dropdown above or below the trigger based on available screen space.',
		"Features built-in scrolling support with customizable <SelectScrollUpButton> and <SelectScrollDownButton> components for handling long option lists safely.",
	]}
	related={[
		{
			title: "Dropdown Menu",
			description:
				"Often confused with Select. Use a Select specifically for picking a data value within a form; use a Dropdown Menu for triggering application actions or URL navigation.",
			href: "/docs/components/dropdown",
		},
		{
			title: "Combobox",
			description:
				"If your list contains hundreds of options (like all global cities), a standard Select becomes unwieldy. A Combobox combines a Select with an integrated text search input.",
			href: "/docs/components/combobox",
		},
		{
			title: "Form",
			description:
				"The absolutely necessary wrapper to integrate the Select into complex Zod validation schemas and display localized error messages.",
			href: "/docs/components/form",
		},
	]}
	faqs={[
		{
			question:
				"What is the exact difference between Select and Dropdown Menu?",
			answer:
				'A Select component is explicitly designed for data collection (it holds a standard value and acts like an input). A Dropdown Menu is designed for commanding the application (like clicking an "Edit Profile" button or opening a modal).',
		},
		{
			question: "How do I integrate this with React Hook Form?",
			answer:
				"Because this is a custom controlled component that doesn't trigger standard native onChange events, you must wrap the root <Select> in a React Hook Form <Controller>. You then map the primitive's custom onValueChange event directly to field.onChange to ensure your state updates accurately.",
		},
		{
			question: "How do I set placeholder text before an option is chosen?",
			answer:
				'You pass a string directly to the placeholder prop on the nested <SelectValue> component (e.g., <SelectValue placeholder="Choose a country" />). The primitive automatically clears this text once a valid selection is made.',
		},
		{
			question: "Can I group related options together?",
			answer:
				"Yes. You can wrap a set of <SelectItem> components inside a <SelectGroup>. To add a non-selectable header to that group, use the <SelectLabel> component, which beautifully styles the category name slightly muted and correctly removes it from keyboard traversal paths.",
		},
		{
			question:
				"Why is my dropdown content rendering far away from the trigger?",
			answer:
				'The Radix primitive defaults to position="item-aligned"+], which attempts to mimic native macOS behavior by positioning the currently selected item directly under the user\'s cursor. For a more standard web UI experience, open your local component file and change the <SelectContent> prop to position="popper".',
		},
	]}
/>

## Props

<PropsTableWrapper>
<PropsTable
	title="Select"
	data={[
		{
			name: "indicatorPosition",
			required: false,
			defaultValue: "right",
			values: ["left", "right"],
			description: "Where to show the indicator.",
		},
		{
			name: "indicator",
			required: false,
			defaultValue: "null",
			values: ["ReactNode"],
			description: "Replace the default indicator with custom indicator",
		},
	]}
	externalReference="https://www.radix-ui.com/primitives/docs/components/select"
/>

<PropsTable
	title="SelectTrigger"
	data={[
		{
			name: "size",
			description: "Sets the size of the select field",
			defaultValue: "md",
			values: ["sm", "md", "lg"],
		},
	]}
	externalReference="https://www.radix-ui.com/primitives/docs/components/select"
/>

<PropsTable
	title="SelectGroup"
	data={[
		{
			name: "label",
			values: ["string"],
			description: "Shows label for select",
		},
	]}
	externalReference="https://www.radix-ui.com/primitives/docs/components/select"
/>

</PropsTableWrapper>
