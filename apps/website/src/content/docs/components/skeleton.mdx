---
title: Skeleton
description: Placeholder loading screen to enhance perceived performance
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/ui/skeleton.tsx
---

<ComponentPreview path="skeleton/skeleton-preview" />

## Installation

<Tabs defaultValue="cli">
	<TabsList>
		<TabsTrigger value="cli">CLI</TabsTrigger>
		<TabsTrigger value="manual">Manual</TabsTrigger>
	</TabsList>
	<TabsContent value="cli" className="p-0">
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm dlx radianui@latest add skeleton",
				npm: "npx radianui@latest add skeleton",
				yarn: "yarn radianui@latest add skeleton",
				bun: "bunx radianui@latest add skeleton",
			}}
		/>
	</TabsContent>
	<TabsContent value="manual" className="p-0">
	<Steps>
	    <Step>Install the following dependencies:</Step>
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm add @emotion/css",
				npm: "npm install @emotion/css",
				yarn: "yarn add @emotion/css",
				bun: "bun add @emotion/css",
			}}
		/>

    	<Step>Copy and paste the following code into your project.</Step>
    	<ComponentSource name="skeleton" title="components/ui/skeleton.tsx" collapsible />

    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </TabsContent>

</Tabs>

## Usage

### Imports

<CodeSnippet title="/components/ui/skeleton" code={`import { Skeleton } from "@/components/ui/skeleton"`} />

### Code

<CodeSnippet title="/components/ui/skeleton" code={`<Skeleton className="w-full h-10" />`} />

---

## Props

<PropsTableWrapper>
<PropsTable
title="Skeleton"
data={
[
    {
        name:"className",
        values:["string"],
        description:"Class name to be applied to the skeleton"
    }
]
}
/>

</PropsTableWrapper>
