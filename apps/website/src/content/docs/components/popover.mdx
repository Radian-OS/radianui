---
title: Popover
description: A small info bubble pinned to a button that pops up to show extra text, settings, or rich interactive content.
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/ui/popover.tsx
  externalReference:
    - label: Radix UI
      href: https://www.radix-ui.com/primitives/docs/components/popover
---

<ComponentPreview path="popover/popover-preview" />

---

## Installation

<InstallationTabs>
	<InstallTabsList>
		<InstallTabsTrigger value="cli">CLI</InstallTabsTrigger>
		<InstallTabsTrigger value="manual">Manual</InstallTabsTrigger>
	</InstallTabsList>
	<InstallTabsContent value="cli">
    ```bash
    npm install @radix-ui/react-popover
    ```
    </InstallTabsContent>
    <InstallTabsContent value="manual" className="p-0">
    	<Steps>
    		<Step>Copy and paste the following code into your project.</Step>
    		<ComponentSource
    			src="styles/default/ui/popover.tsx"
    			title="components/ui/popover.tsx"
    			collapsible
    		/>
    		<Step>Update the import paths as per your project structure.</Step>
    	</Steps>
    </InstallTabsContent>

</InstallationTabs>

---

## Usage

### Imports

```tsx showLineNumbers title="/components/ui/popover"
import {
	Popover,
	PopoverContent,
	PopoverTrigger,
} from "@/components/ui/popover"
```

### Code

```tsx showLineNumbers title="/components/ui/popover"
<Popover>
	<PopoverTrigger>Open Popover</PopoverTrigger>
	<PopoverContent>This is a simple popover content</PopoverContent>
</Popover>
```

---

## Examples

### Side

<ComponentPreview path="popover/popover-side" />

### Align

<ComponentPreview path="popover/popover-align" />

### Guided Tour

<ComponentPreview path="popover/popover-guide" />

### Emoji Picker with frimousse

Lightweight and fast, focused on core emojis with minimal overhead. Best for
performance-sensitive UI.

<ComponentPreview path="popover/popover-emoji" />

### Emoji Picker with emoji-mart

Feature-rich, with recents and category tabs. Best when functionality and
browsing options are priorities.

<ComponentPreview path="popover/popover-emoji2" />

---

<ComponentDocContent
	name={"Popover"}
	description={
		"Popover is a floating panel that displays interactive content, such as forms, calendars, or additional details, next to a trigger element.It provides smart positioning, and customizable animations for a smooth user experience."
	}
	features={[
		"@radix-ui/react-popover, ensures strict ARIA compliance, automatic focus trapping within the overlay, and native Esc key dismissal.",
		"Renders inside a React Portal to completely avoid overflow clipping issues caused by parent CSS containers.",
		"Collision-aware positioning via the underlying Popper engine automatically flips or shifts the overlay if it hits the edge of the user's viewport.",
		"Exposes highly composable, modular pieces: <Popover>, <PopoverTrigger> and <PopoverContent>.",
		"Includes smooth Tailwind CSS keyframe animations (like animate-in, zoom-in, and fade-in) that trigger perfectly when the popover mounts to the DOM.",
	]}
	related={[
		{
			title: "Tooltip",
			description:
				"Use a Tooltip for brief, plain-text helper labels that appear on hover. Use a Popover when you need to render rich, interactive HTML content that requires a click to open.",
			href: "/docs/components/tooltip",
		},
		{
			title: "Dialog",
			description:
				"Use a Dialog (modal) when you want to explicitly interrupt the user's workflow, dim the background, and force them to complete an action.",
			href: "/docs/components/dialog",
		},
		{
			title: "Dropdown Menu",
			description:
				"Structurally similar, but explicitly designed for rendering a vertical list of application actions. A Popover is unopinionated about its internal layout.",
			href: "/docs/components/dropdown",
		},
	]}
	faqs={[
		{
			question: "How do I change which side the popover opens on?",
			answer:
				'You can pass standard Popper layout props directly to the <PopoverContent> component. For example, use side="top" or side="right" to dictate the preferred placement, and use align="start" | "center" | "end" to adjust how it aligns with the trigger button.',
		},
		{
			question: "Does it close automatically when I click outside of it?",
			answer:
				'Yes, by default, standard "outside click" interactions dismiss the popover.',
		},
		{
			question:
				"Can I place input fields or standard HTML forms inside the popover?",
			answer:
				"Absolutely. Because the primitive correctly traps focus and manages DOM events, nesting text inputs, checkboxes, or composite components like the Radian Date Picker inside a popover is completely accessible and highly recommended for building complex UI filters.",
		},
		{
			question: "Why is my popover rendering underneath my sticky site header?",
			answer:
				"While the menu uses a React Portal to escape local container bounds, it still inherits standard stacking contexts. If your site header has an extremely high z-index, open your local popover.tsx file and verify that the <PopoverContent> includes a utility like z-50 to compete with your global layout.",
		},
	]}
/>

## API Reference

### Popover

The root component. Alias for `Popover.Root` from Radix UI.

    ### PopoverTrigger

An interactive element that opens the popover when clicked.

    ### PopoverContent

The floating panel that appears when the popover is open.

<PropsTable
	data={[
		{
			name: "align",
			values: ["start", "center", "end"],
			defaultValue: "center",
			required: false,
			description: "The alignment of the content relative to the trigger.",
		},
		{
			name: "sideOffset",
			values: ["number"],
			defaultValue: "4",
			required: false,
			description:
				"The distance in pixels between the trigger and the popover content.",
		},
	]}
/>
