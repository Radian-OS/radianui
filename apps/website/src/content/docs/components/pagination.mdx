---
title: Pagination
description: Display active page and navigate between multiple pages
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/ui/pagination.tsx
---

<PaginationPreview />

## Installation

<Tabs defaultValue="cli">
	<TabsList>
		<TabsTrigger value="cli">CLI</TabsTrigger>
		<TabsTrigger value="manual">Manual</TabsTrigger>
	</TabsList>
	<TabsContent value="cli" className="p-0">
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm dlx @radianos/radianbeta add pagination",
				npm: "npx @radianos/radianbeta add pagination",
				yarn: "yarn @radianos/radianbeta add pagination",
				bun: "bunx @radianos/radianbeta add pagination",
			}}
		/>
	</TabsContent>
	<TabsContent value="manual" className="p-0">
	<Steps>
	    <Step>Install the following dependencies:</Step>
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm add @radix-ui/react-pagination",
				npm: "npm install @radix-ui/react-pagination",
				yarn: "yarn add @radix-ui/react-pagination",
				bun: "bun add @radix-ui/react-pagination",
			}}
		/>

    	<Step>Copy and paste the following code into your project.</Step>
    	<ComponentSource name="pagination" title="components/ui/pagination.tsx" collapsible />

    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </TabsContent>

</Tabs>

## Usage

### Imports

<CodeSnippet title="/components/ui/pagination" code={`import Pagination from "@/components/ui/pagination"`} />

### Code

<CodeSnippet
	title="/components/ui/pagination"
	code={`<Pagination
		currentPage={2}
		totalPage={20}
		rowPerPage={true}
		goToPage={true}
		enableCustomRows={true}
		customRows={5}
		navButton="both"
/>`}
/>

---

## Props

<PropsTableWrapper>
	<PropsTable
		title="Pagination"
		data={[
			{
				name: "currentPage",
				description: "Displays current page or index",
				values: ["number"],
				
			},
			{
				name: "totalPage",
				description: "Displays total number of pages",
				values: ["number"],
    		},
			{
				name:"navButton",
				description: "Displays the type of media in pagination",
				values:["icon","text","both"],
				defaultValue:"icon"
			},
			
    	]}
    />

</PropsTableWrapper>
