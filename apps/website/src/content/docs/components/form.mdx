---
title: Form
description: Collect user inputs and validate them in structured fields
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/ui/hover-card.tsx
---

<ComponentPreview path="form/form-preview" />

## Installation

`zod` and `react-hook-form` are used together for simple, type-safe validation.

<br />

`zod` defines the schema and rules, while `react-hook-form` applies them in real time, reducing boilerplate and keeping forms consistent and easy to maintain.

<br />

<Tabs defaultValue="cli">
	<TabsList>
		<TabsTrigger value="cli">CLI</TabsTrigger>
		<TabsTrigger value="manual">Manual</TabsTrigger>
	</TabsList>
	<TabsContent value="cli" className="p-0">
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm dlx @radianos/radianbeta add form",
				npm: "npx @radianos/radianbeta add form",
				yarn: "yarn @radianos/radianbeta add form",
				bun: "bunx @radianos/radianbeta add form",
			}}
		/>
	</TabsContent>
	<TabsContent value="manual" className="p-0">
	<Steps>

    	<Step>Copy and paste the following code into your project.</Step>
    		<ComponentSource name="form" title="components/ui/form.tsx" collapsible />
    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </TabsContent>

</Tabs>

## Usage

These components are also integrated with form for validation and state management.

- [Checkbox](/docs/components/checkbox#form)
- [One Time Password Field](/docs/components/one-time-password-field#form)
- [Radio Group](/docs/components/radio-group#form)
- [Select](/docs/components/select#form)
- [Text Area](/docs/components/text-area#form)

### Imports

<CodeSnippet
	title="/components/ui/form.tsx"
	code={`import {
Form, 
FormControl, 
FormDescription, 
FormField, 
FormItem, 
FormLabel, 
FormMessage
} from '@/components/ui/form'`}
/>

### Code

<CodeSnippet
	title="/components/ui/form.tsx"
	code={`<Form>
	<FormField
		render={({ field }) => (
			<FormItem>
				<FormLabel>Email</FormLabel>
				<FormControl>
					<Input placeholder="Email address" {...field} />
				</FormControl>
				<FormDescription>Enter your email</FormDescription>
				<FormMessage />
			</FormItem>
		)} 
	/>
</Form>`}
/>

---

## Examples

### Sign up form

<ComponentPreview path="form/sign-up-form" />

### Sign in form

<ComponentPreview path="form/sign-in-form" />

---

## Props

<PropsTableWrapper>
	<PropsTable
		title="Form"
		data={[
			{
				name: "children",
				values: ["React.ReactNode"],
				required: true,
				description: "Form content wrapped by the FormProvider context.",
			},
		]}
	/>
	<PropsTable
		title="FormField"
		data={[
			{
				name: "name",
				required: true,
				description: "The name of the field in the form schema.",
			},
			{
				name: "control",
				required: true,
				description: "Control object from react-hook-form's useForm hook.",
			},
			{
				name: "render",
				required: true,
				description: "Render function that receives field props and state.",
			},
		]}
	/>
	<PropsTable
		title="FormItem"
		data={[
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Additional CSS classes for the form item container.",
			},
			{
				name: "children",
				values: ["React.ReactNode"],
				required: true,
				description: "Form item content including label, control, description, and message.",
			},
		]}
	/>
	<PropsTable
		title="FormLabel"
		data={[
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Additional CSS classes for the form label.",
			},
			{
				name: "children",
				values: ["React.ReactNode"],
				required: true,
				description: "Label text or content.",
			},
		]}
	/>
	<PropsTable
		title="FormControl"
		data={[
			{
				name: "children",
				values: ["React.ReactElement"],
				required: true,
				description: "Single child element that will receive form control props.",
			},
			{
				name: "asChild",
				values: ["boolean"],
				required: false,
				description: "When true, merges props with the child element instead of wrapping it.",
			},
		]}
	/>
	<PropsTable
		title="FormDescription"
		data={[
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Additional CSS classes for the form description.",
			},
			{
				name: "children",
				values: ["React.ReactNode"],
				required: true,
				description: "Description text or content. Hidden when field has an error.",
			},
		]}
	/>
	<PropsTable
		title="FormMessage"
		data={[
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Additional CSS classes for the form message.",
			},
			{
				name: "children",
				values: ["React.ReactNode"],
				required: false,
				description: "Custom message content. Displays error message if present, otherwise shows children.",
			},
		]}
	/>
</PropsTableWrapper>
