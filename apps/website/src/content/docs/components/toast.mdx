---
title: Toast
description: Display non-intrusive notifications or alerts
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/ui/toast.tsx
  externalReference:
    - label: Sonner
      href: https://sonner.emilkowal.ski/
---

<ToastPreview />

## Installation

<Tabs defaultValue="cli">
	<TabsList>
		<TabsTrigger value="cli">CLI</TabsTrigger>
		<TabsTrigger value="manual">Manual</TabsTrigger>
	</TabsList>
	<TabsContent value="cli" className="p-0">
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm dlx @radianos/radianbeta add toast",
				npm: "npx @radianos/radianbeta add toast",
				yarn: "yarn @radianos/radianbeta add toast",
				bun: "bunx @radianos/radianbeta add toast",
			}}
		/>
	</TabsContent>
	<TabsContent value="manual" className="p-0">
	<Steps>
	    <Step>Install the following dependencies:</Step>
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm add sonner @radix-ui/react-slot",
				npm: "npm install sonner @radix-ui/react-slot",
				yarn: "yarn add sonner @radix-ui/react-slot",
				bun: "bun add sonner @radix-ui/react-slot",
			}}
		/>

    	<Step>Copy and paste the following code into your project.</Step>
    	<Tabs defaultValue="toast">
    		<TabsList>
    			<TabsTrigger value="toast">Toast</TabsTrigger>
    			<TabsTrigger value="button">Button</TabsTrigger>
    			<TabsTrigger value="spinner">Spinner</TabsTrigger>
    		</TabsList>
    		<TabsContent value="toast">
    			<ComponentSource name="toast" title="components/ui/toast.tsx" collapsible />
    		</TabsContent>
    		<TabsContent value="button">
    			<ComponentSource name="button" title="components/ui/button.tsx" collapsible />
    		</TabsContent>
    		<TabsContent value="spinner">
    			<ComponentSource name="spinner" title="components/ui/spinner.tsx" collapsible />
    		</TabsContent>
    	</Tabs>

    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </TabsContent>

</Tabs>

## Usage

### Imports

<CodeSnippet title="/components/ui/toast" code={`import { Toaster, showToast } from "@/components/ui/toast"`} />

### Code

<CodeSnippet title="Usage" code={`<Toaster />`} />

---

## Props

<PropsTableWrapper>
	<PropsTable
		title="Toaster"
		data={[
			{
				name: "position",
				values: ["top-left", "top-center", "top-right", "bottom-left", "bottom-center", "bottom-right"],
				defaultValue: ["bottom-right"],
				description: "Sets the position of toast",
			},
			{
				name: "visibleToasts",
				values: ["number"],
				defaultValue: ["3"],
				description: "Sets the number of visible toasts",
			},
			{
				name: "expand",
				values: ["boolean"],
				defaultValue: ["false"],
				description: "Sets the number of expanded toasts",
			},
		]}
	/>
</PropsTableWrapper>
