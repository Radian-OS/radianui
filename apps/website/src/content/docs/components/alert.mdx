---
title: Alert
description: Display important messages with visual emphasis
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/ui/alert.tsx
---

<AlertPreview />

## Installation

<Tabs defaultValue="cli">
	<TabsList>
		<TabsTrigger value="cli">CLI</TabsTrigger>
		<TabsTrigger value="manual">Manual</TabsTrigger>
	</TabsList>
	<TabsContent value="cli" className="p-0">
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm dlx @radianos/radianbeta add alert",
				npm: "npx @radianos/radianbeta add alert",
				yarn: "yarn @radianos/radianbeta add alert",
				bun: "bunx @radianos/radianbeta add alert",
			}}
		/>
	</TabsContent>
	<TabsContent value="manual" className="p-0">
	<Steps>
    	<Step>Copy and paste the following code into your project.</Step>
    	<ComponentSource name="alert" title="components/ui/alert.tsx" collapsible />

    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </TabsContent>

</Tabs>

## Usage

### Imports

<CodeSnippet title="/components/ui/alert.tsx" code={`import {Alert} from "@/components/ui/alert"`} />

### Code

<CodeSnippet
	title="/components/ui/alert.tsx"
	code={`<Alert
      color="info"
      variant="bordered"
      title="Information"
      message="This is an informational alert with an icon."
    />`}
/>

---

## Examples

### Alert Example

<AlertExamplePreview />

### Props

<PropsTableWrapper>
	<PropsTable
		title="Alert"
		data={[
			{
				name: "color",
				defaultValue: "neutral",
				values: ["neutral", "primary", "info", "success", "warning", "error"],
				required: false,
				description: "Sets the color scheme of the alert",
			},
			{
				name: "variant",
				defaultValue: "default",
				values: ["default", "soft-outline", "outline"],
				required: false,
				description: "Defines the visual style of the accordion",
			},
			{
				name: "title",
				values: ["string"],
				required: false,
				description: "The title of the alert, displayed prominently",
			},
			{
				name: "message",
				values: ["string"],
				required: false,
				description: "The message content of the alert",
			},
			{
				name: "start",
				values: ["ReactNode"],
				required: false,
				description: "An optional icon to display alongside the alert",
			},
			{
				name: "end",
				values: ["ReactNode"],
				required: false,
				description: "An optional element to display at the end of the alert",
			},
			{
				name: "closable",
				defaultValue: "false",
				values: ["boolean"],
				required: false,
				description: "If true, the alert can be closed by the user",
			},
		]}
	/>
</PropsTableWrapper>
