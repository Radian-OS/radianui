---
title: Alert
description: Display important messages with visual emphasis
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/ui/alert.tsx
---

<ComponentPreview path="alert/alert-preview" />

## Installation

<Tabs defaultValue="cli">
	<TabsList>
		<TabsTrigger value="cli">CLI</TabsTrigger>
		<TabsTrigger value="manual">Manual</TabsTrigger>
	</TabsList>
	<TabsContent value="cli" className="p-0">
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm dlx @radianos/radianbeta add alert",
				npm: "npx @radianos/radianbeta add alert",
				yarn: "yarn @radianos/radianbeta add alert",
				bun: "bunx @radianos/radianbeta add alert",
			}}
		/>
	</TabsContent>
	<TabsContent value="manual" className="p-0">
	<Steps>
    	<Step>Copy and paste the following code into your project.</Step>
    	<ComponentSource name="alert" title="components/ui/alert.tsx" collapsible />

    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </TabsContent>

</Tabs>

## Usage

### Imports

<CodeSnippet title="/components/ui/alert.tsx" code={`import { Alert, AlertContent, AlertDescription, AlertIcon, AlertTitle,AlertToolbar } from "@/components/ui/alert"`} />

### Code

<CodeSnippet
	title="/components/ui/alert.tsx"
	code={`<Alert>
 <AlertIcon>
  <Bell size={16} />
 </AlertIcon>
 
 <AlertContent>
  <AlertTitle>Alert Title</AlertTitle>
 <AlertDescription>This is alert description.</AlertDescription>
 </AlertContent>
			
 <AlertToolbar>
  <LinkButton href="/">Action</LinkButton>
 </AlertToolbar>
</Alert>`}
/>

---

## Examples

### Variants

<ComponentPreview height="480" path="alert/alert-variants-example" />

### Colors

<ComponentPreview height="500" path="alert/alert-colors-example" />

### Close Icon

<ComponentPreview path="alert/alert-close-example-preview" />

### Props

<PropsTableWrapper>
	<PropsTable
		title="Alert"
		data={[
			{
				name: "color",
				defaultValue: "primary",
				values: ["neutral", "primary", "info", "success", "warning", "error"],
				required: false,
				description: "Sets the color scheme of the alert",
			},
			{
				name: "variant",
				defaultValue: "soft",
				values: ["soft", "soft-outline", "outline", "strong"],
				required: false,
				description: "Defines the visual style of the alert",
			},
			{
				name: "close",
				defaultValue: "false",
				values: ["boolean"],
				required: false,
				description: "Shows a close button to dismiss the alert",
			},
			{
				name: "onClose",
				defaultValue: "-",
				values: ["() => void"],
				required: false,
				description: "Callback function called when the alert is dismissed",
			},
		]}
	/>
	<PropsTable
		title="AlertIcon"
		data={[
			{
				name: "children",
				defaultValue: "-",
				values: ["ReactNode"],
				required: false,
				description: "Pass an icon component as children to display in the alert",
			},
		]}
	/>
	<PropsTable
		title="AlertTitle"
		data={[
			{
				name: "children",
				defaultValue: "-",
				values: ["ReactNode"],
				required: false,
				description: "The title text content of the alert",
			},
		]}
	/>
	<PropsTable
		title="AlertDescription"
		data={[
			{
				name: "children",
				defaultValue: "-",
				values: ["ReactNode"],
				required: false,
				description: "The description/body content of the alert",
			},
		]}
	/>
	<PropsTable
		title="AlertContent"
		data={[
			{
				name: "children",
				defaultValue: "-",
				values: ["ReactNode"],
				required: false,
				description: "Content wrapper area that typically contains AlertTitle and AlertDescription",
			},
		]}
	/>
	<PropsTable
		title="AlertToolbar"
		data={[
			{
				name: "children",
				defaultValue: "-",
				values: ["ReactNode"],
				required: false,
				description: "Custom actions or buttons to display on the right side of the alert",
			},
		]}
	/>
</PropsTableWrapper>
