---
title: Stepper
description: A component that displays a series of steps and allows users to navigate between them
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/ui/stepper.tsx
---

<ComponentPreview path="stepper/stepper-preview" />

## Installation

<InstallationTabs defaultValue="cli">
	<InstallTabsList>
		<InstallTabsTrigger value="cli">CLI</InstallTabsTrigger>
		<InstallTabsTrigger value="manual">Manual</InstallTabsTrigger>
	</InstallTabsList>
	<InstallTabsContent value="cli" className="p-0">
		```bash
		npx radianui@latest add stepper
		```
	</InstallTabsContent>
	<InstallTabsContent value="manual" className="p-0">
	<Steps>
    	<Step>Copy and paste the following code into your project.</Step>
    	<ComponentSource name="stepper" title="components/ui/stepper.tsx" collapsible />

    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </InstallTabsContent>

</InstallationTabs>

## Usage

### Imports

```tsx showLineNumbers title="/components/ui/stepper.tsx"
import {
	Stepper,
	StepperContent,
	StepperDescription,
	StepperIndicator,
	StepperItem,
	StepperNav,
	StepperPanel,
	StepperSeparator,
	StepperTitle,
	StepperTrigger,
} from "@/components/ui/stepper"
```

### Code

```tsx showLineNumbers title="stepper-demo.tsx"
const steps = [
	{ step: 1, title: "Cart", description: "Review items" },
	{ step: 2, title: "Shipping", description: "Address details" },
	{ step: 3, title: "Payment", description: "Billing method" },
]

<Stepper defaultValue={1} className="space-y-6">
	<StepperNav>
		{steps.map((item, index) => (
			<StepperItem key={item.step} step={item.step}>
				<StepperTrigger>
					<StepperIndicator>{item.step}</StepperIndicator>
					<span className="text-left">
						<StepperTitle>{item.title}</StepperTitle>
						<StepperDescription>{item.description}</StepperDescription>
					</span>
				</StepperTrigger>
				{index < steps.length - 1 && <StepperSeparator />}
			</StepperItem>
		))}
	</StepperNav>

	<StepperPanel>
		{steps.map((item) => (
			<StepperContent key={item.step} value={item.step}>
				{item.title}
			</StepperContent>
		))}
	</StepperPanel>
</Stepper>
```

---

## Examples

#### With complete states

<ComponentPreview title="With complete states" path="stepper/stepper-state" />
#### With control button

<ComponentPreview
	title="With control button"
	path="stepper/stepper-controlled"
/>
#### Stepper with Title

<ComponentPreview title="Stepper with Title" path="stepper/stepper-title" />
#### Stepper with Description

<ComponentPreview
	title="Stepper with Description"
	path="stepper/stepper-description"
/>
#### Stepper with Icon

<ComponentPreview title="Stepper with Icon" path="stepper/stepper-icon" />
#### With Progress Bar

<ComponentPreview title="With Progress Bar" path="stepper/stepper-bar" />
#### Vertical Stepper

<ComponentPreview title="Vertical Stepper" path="stepper/stepper-vertical" />

---

## Props

<PropsTableWrapper>
	<PropsTable
		title="Stepper"
		data={[
			{
				name: "defaultValue",
				values: ["number"],
				required: false,
				description: "The initial active step when the stepper is uncontrolled",
				defaultValue: "1",
			},
			{
				name: "value",
				values: ["number"],
				required: false,
				description: "The controlled active step value",
			},
			{
				name: "onValueChange",
				values: ["(value: number) => void"],
				required: false,
				description:
					"Callback fired when a step trigger changes the active step",
			},
			{
				name: "orientation",
				values: ["horizontal", "vertical"],
				required: false,
				description: "Controls the direction of the step navigation",
				defaultValue: "horizontal",
			},
			{
				name: "indicators",
				values: ["StepIndicators"],
				required: false,
				description:
					"Custom indicator nodes for active, completed, inactive, and loading states",
			},
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Additional CSS classes for the root stepper",
			},
		]}
	/>
	<PropsTable
		title="StepperItem"
		data={[
			{
				name: "step",
				values: ["number"],
				required: true,
				description: "Numeric value represented by the step item",
			},
			{
				name: "completed",
				values: ["boolean"],
				required: false,
				description: "Forces the item into the completed state",
				defaultValue: "false",
			},
			{
				name: "disabled",
				values: ["boolean"],
				required: false,
				description: "Disables the step trigger for the item",
				defaultValue: "false",
			},
			{
				name: "loading",
				values: ["boolean"],
				required: false,
				description: "Shows the loading indicator when this item is active",
				defaultValue: "false",
			},
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Additional CSS classes for the step item",
			},
		]}
	/>
	<PropsTable
		title="StepperTrigger"
		data={[
			{
				name: "asChild",
				values: ["boolean"],
				required: false,
				description: "Renders the trigger wrapper as a child slot-like element",
				defaultValue: "false",
			},
			{
				name: "disabled",
				values: ["boolean"],
				required: false,
				description: "Disables the trigger button",
			},
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Additional CSS classes for the trigger",
			},
		]}
	/>
	<PropsTable
		title="StepperIndicator"
		data={[
			{
				name: "children",
				values: ["ReactNode"],
				required: false,
				description:
					"Fallback indicator content when no custom indicator is set",
			},
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Additional CSS classes for the indicator",
			},
		]}
	/>
	<PropsTable
		title="StepperSeparator"
		data={[
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Additional CSS classes for the separator",
			},
		]}
	/>
	<PropsTable
		title="StepperTitle"
		data={[
			{
				name: "children",
				values: ["ReactNode"],
				required: false,
				description: "Title content for a step",
			},
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Additional CSS classes for the title",
			},
		]}
	/>
	<PropsTable
		title="StepperDescription"
		data={[
			{
				name: "children",
				values: ["ReactNode"],
				required: false,
				description: "Description content for a step",
			},
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Additional CSS classes for the description",
			},
		]}
	/>
	<PropsTable
		title="StepperNav"
		data={[
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Additional CSS classes for the step navigation",
			},
		]}
	/>
	<PropsTable
		title="StepperPanel"
		data={[
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Additional CSS classes for the content panel",
			},
		]}
	/>
	<PropsTable
		title="StepperContent"
		data={[
			{
				name: "value",
				values: ["number"],
				required: true,
				description: "Step value that controls when this content is shown",
			},
			{
				name: "forceMount",
				values: ["boolean"],
				required: false,
				description:
					"Keeps the content mounted and hides it when its step is inactive",
				defaultValue: "false",
			},
			{
				name: "className",
				values: ["string"],
				required: false,
				description: "Additional CSS classes for the step content",
			},
		]}
	/>
</PropsTableWrapper>
