---
title: Fade
description: Add fade in/out animation to any component or text. Supports blur too.
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/animated/fade.tsx
  externalReference:
    - label: Motion
      href: https://motion.dev/docs
      icon: motion.webp
---

<FadePreview />

## Installation

<Tabs defaultValue="cli">
	<TabsList>
		<TabsTrigger value="cli">CLI</TabsTrigger>
		<TabsTrigger value="manual">Manual</TabsTrigger>
	</TabsList>
	<TabsContent value="cli" className="p-0">
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm dlx radianui@latest add fade",
				npm: "npx radianui@latest add fade",
				yarn: "yarn radianui@latest add fade",
				bun: "bunx radianui@latest add fade",
			}}
		/>
	</TabsContent>
	<TabsContent value="manual" className="p-0">
	<Steps>
	    <Step>Install the following dependencies:</Step>
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm add motion",
				npm: "npm install motion",
				yarn: "yarn add motion",
				bun: "bun add motion",
			}}
		/>

    	<Step>Copy and paste the following code into your project.</Step>
    	<ComponentSource name="fade" title="components/ui/fade.tsx" collapsible />

    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </TabsContent>

</Tabs>

## Usage

### Imports

<CodeSnippet title="/components/animated/fade" code={`import { Fade } from "@/components/animated/fade"`} showLineNumbers />

### Code

<CodeSnippet
	title="/components/animated/fade"
	code={`<Fade>
		<p>Hello, World!</p>
</Fade>`}
	showLineNumbers
/>

---

## Examples

### Blur Fade Example

<BlurFadeExample />

### Fade Out Example

<FadeOutExample />

## Props

<PropsTableWrapper>
	<PropsTable
		title="Fade"
		data={[
			{
				name: "ease",
				values: ["EaseOption"],
				defaultValue: "easeOut",
				description: "Easing function for the fade animation.",
				required: false,
			},
			{
				name: "duration",
				values: ["number"],
				defaultValue: "0.6",
				description: "Duration of the fade animation in seconds.",
				required: false,
			},
			{
				name: "offset",
				values: ["number"],
				defaultValue: "10",
				description: "How far (px) the element starts offset before fading in.",
				required: false,
			},
			{
				name: "blur",
				values: ["string"],
				defaultValue: "0px",
				description: "Amount of blur applied at the start of the animation.",
				required: false,
			},
			{
				name: "direction",
				values: ["up", "down", "left", "right"],
				defaultValue: "up",
				description: "Direction from which the element fades in.",
				required: false,
			},
			{
				name: "delay",
				values: ["number"],
				defaultValue: "0",
				description: "Delay before the animation starts (in seconds).",
				required: false,
			},
			{
				name: "variants",
				values: ["Variants"],
				defaultValue: "-",
				description: "Custom Framer Motion variants to override default fade behavior.",
				required: false,
			},
			{
				name: "inView",
				values: ["boolean"],
				defaultValue: "false",
				description: "If true, animation only plays when the element enters the viewport.",
				required: false,
			},
			{
				name: "inViewMargin",
				values: ["string"],
				defaultValue: "-50px",
				description: "Margin used for the Intersection Observer when `inView` is enabled.",
				required: false,
			},
			{
				name: "isVisible",
				values: ["boolean"],
				defaultValue: "true",
				description: "Controls whether the element is visible. Useful for conditional rendering.",
				required: false,
			},
		]}
	/>
</PropsTableWrapper>
