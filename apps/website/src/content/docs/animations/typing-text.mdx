---
title: Typing Text
description: Characters appearing in typed animation
links:
  github:
    href: https://github.com/Radian-os/radianos/blob/main/apps/website/src/registry/animated/typing-text.tsx
  externalReference:
    - label: Motion
      href: https://motion.dev/docs
      icon: motion.webp
---

<TypingTextPreview />

## Installation

<Tabs defaultValue="cli">
	<TabsList>
		<TabsTrigger value="cli">CLI</TabsTrigger>
		<TabsTrigger value="manual">Manual</TabsTrigger>
	</TabsList>
	<TabsContent value="cli" className="p-0">
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm dlx @radianos/radianbeta add typing-text",
				npm: "npx @radianos/radianbeta add typing-text",
				yarn: "yarn @radianos/radianbeta add typing-text",
				bun: "bunx @radianos/radianbeta add typing-text",
			}}
		/>
	</TabsContent>
	<TabsContent value="manual" className="p-0">
	<Steps>
	    <Step>Install the following dependencies:</Step>
		<PackageManagerTabs
			commands={{
				pnpm: "pnpm add motion",
				npm: "npm install motion",
				yarn: "yarn add motion",
				bun: "bun add motion",
			}}
		/>

    	<Step>Copy and paste the following code into your project.</Step>
    	<ComponentSource name="typing-text" title="components/ui/typing-text.tsx" collapsible />

    	<Step>Update the import paths as per your project structure.</Step>
    </Steps>
    </TabsContent>

</Tabs>

## Usage

### Imports

<CodeSnippet title="/components/animated/typing-text" code={`import { TypingText } from "@/components/animated/typing-text"`} showLineNumbers />

### Code

<CodeSnippet title="/components/animated/typing-text" code={`<TypingText>Your text here</TypingText>`} showLineNumbers />

---

## Props

<PropsTableWrapper>
	<PropsTable
		title="TypingText"
		data={[
			{
				name: "texts",
				values: ["string[]"],
				description: "An array of strings that will be typed out in sequence.",
				required: true,
			},
			{
				name: "duration",
				values: ["number"],
				defaultValue: "0.05",
				description: "Duration of animation of one character.",
				required: false,
			},
			{
				name: "deleteSpeed",
				values: ["number"],
				defaultValue: "0.03",
				description: "Speed at which characters are deleted(in seconds).",
				required: false,
			},
			{
				name: "delay",
				values: ["number"],
				defaultValue: "-",
				description: "Delay before typing starts(in seconds).",
				required: false,
			},
			{
				name: "pauseBetween",
				values: ["number"],
				defaultValue: "1.5",
				description: "Pause duration between typing different texts (in seconds).",
				required: false,
			},
			{
				name: "ease",
				values: ["EaseOption"],
				defaultValue: "linear",
				description: "Easing function to control typing speed variations.",
				required: false,
			},
			{
				name: "showCursor",
				values: ["boolean"],
				defaultValue: "true",
				description: "Whether to display the typing cursor.",
				required: false,
			},
			{
				name: "cursorDuration",
				values: ["number"],
				defaultValue: "0.8",
				description: "Blinking interval for the cursor (in seconds).",
				required: false,
			},
			{
				name: "cursorChar",
				values: ["string"],
				defaultValue: "|",
				description: "Character used as the cursor.",
				required: false,
			},
			{
				name: "loop",
				values: ["boolean"],
				defaultValue: "true",
				description: "If true, typing will loop through texts indefinitely.",
				required: false,
			},
			{
				name: "onComplete",
				values: ["function"],
				description: "Callback function triggered when typing finishes (if not looping).",
				required: false,
			},
			{
				name: "as",
				values: ["React.ElementType"],
				defaultValue: "span",
				description: "Custom component or HTML element to render as.",
				required: false,
			},
		]}
	/>
</PropsTableWrapper>
