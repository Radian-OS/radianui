import React from "react"
import { Alert } from "@/registry/ui/alert"

const AlertLucide = () => {
	return <Alert variant="soft" color="warning" title="Native Icon set" />
}

export default AlertLucide
