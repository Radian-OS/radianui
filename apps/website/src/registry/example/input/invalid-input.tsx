import React from "react"
import { Input } from "@/registry/ui/input"

const InvalidInput = () => {
	return <Input className="w-80" aria-invalid placeholder="Disabled Input" />
}

export default InvalidInput
