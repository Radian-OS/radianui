import { Geist, Inter, Manrope, Open_Sans, Roboto } from "next/font/google"

export const inter = Inter({ subsets: ["latin"], display: "swap" })
export const roboto = Roboto({ subsets: ["latin"], display: "swap" })
export const openSans = Open_Sans({ subsets: ["latin"], display: "swap" })
export const manrope = Manrope({ subsets: ["latin"], display: "swap" })
export const geist = Geist({ subsets: ["latin"], display: "swap" })
